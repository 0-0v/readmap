# Book 도메인 원칙

## 목적

Book 도메인은 사용자의 독서 기록을 관리한다. 책 정보는 카카오 도서 API에서 가져오고, 독서 메타데이터(별점, 기간, 국가, 메모)와 함께 Supabase에 저장한다.

## 원칙

- 책 정보(제목, 저자, 출판사, 표지, ISBN)는 카카오 도서 API에서 가져온다. 직접 입력하지 않는다
- 독서 기록은 사용자별로 격리된다. 다른 사용자의 데이터에 접근하지 않는다
- 한 권의 책은 하나의 원산지 국가를 가진다
- 책 삭제는 복구할 수 없다
